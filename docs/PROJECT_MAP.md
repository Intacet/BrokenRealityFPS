# Project map

## Known state

- Reload/equip/ADS animation-recovery patch exception: ViewModelController and Constants in src now intentionally differ from the captured baseline. See RELOAD_DIAGNOSIS.md (including its "Extended scope" section covering the equip and ADS additions). Historical statements below about byte-identical src describe the initial migration capture; import/studio-snapshot remains unchanged. Studio has not received this patch.
- Wood/door destruction exception: two new files exist under src/ServerScriptService/Services (DestructionService.lua, DestructionRules.lua), and GunService.server.lua + Constants.lua now also intentionally differ from the captured baseline for this reason. See docs/DESTRUCTION_SYSTEM_PLAN.md. Studio has not received this either. No new remote was added — the pre-existing, previously-unused PartDestroyed RemoteEvent (declared in RemoteSetup.server.lua) now has a real producer.
- Damage test dummy exception (Stages 1–6): new server files under src/ServerScriptService/Services — CombatEvents.lua, DamageRules.lua, DummyService.server.lua, BloodService.server.lua, HitReactionService.server.lua. New client files under src/StarterPlayer/StarterPlayerScripts/Controllers — BloodController.lua and UI/DummyDebugUI.lua (both registered in ClientInit.client.lua). DamageService.lua, RagdollService.lua, GunService.server.lua, RemoteSetup.server.lua, Constants.lua and Types.lua intentionally differ from baseline; new helper scripts/Build-TestDummy.luau. CombatEvents holds server-side BindableEvents DamageDealt / EntityKilled (DamageService is the sole producer). See docs/DAMAGE_TEST_DUMMY_PLAN.md. Studio has not received this.
- New RemoteEvents (added to RemoteSetup.server.lua): BloodEffect (server → all clients, producer BloodService, consumer BloodController); DummyDevCommand (client → server, producer DummyDebugUI, consumer DummyService, dev-gated); DummyDevState (server → subscribed dev client, producer DummyService, consumer DummyDebugUI). Global blood kill switch is the BR_BloodGlobalEnabled attribute on ReplicatedStorage, not a remote.
- Tarkov-style hipfire (2026-09-09): `GunController` fires the hipfire raycast from the **camera position** along the direction the free-aim-rotated gun visually points — `baseDir = (camera.CFrame * CFrame.Angles(ViewModelController.GetFreeAimAngles())).LookVector` — so bullets leave where the gun points, not a camera ray through a moving reticle. ADS / free-aim-off fall back to the camera ray through the reticle offset. Gated by `Constants.FREE_AIM_FIRE_FROM_MUZZLE`. Wires the old DEBT-063. **(2026-09-09 revision)** The earlier version fired from the viewmodel `MuzzleAttachment.WorldCFrame`; in Studio that attachment (auto-created on the AKS-74 `Barrel`) pointed ~180° backwards and its Model pivot was detached far from the player, so the server rejected every hipfire shot (`WeaponFired: origin too far from root`). Firing from the camera along the gun's rotation removes the dependency on that cosmetic attachment and keeps the origin server-valid. `ViewModelController.GetMuzzleWorldCFrame()` remains as a public accessor but is no longer used by the fire path; the auto-create sign heuristic in `setupMuzzleFx` was also fixed (bore axis now points away from the model centroid) so the muzzle-flash emitters face forward. Per-weapon feel via `Constants.FREE_AIM_PROFILES` (`DEFAULT` + `AKS74` + `AR15`), resolved in `FreeAimController:SetWeapon(name)` and `ViewModelController` on equip. The OS cursor is locked to centre + hidden while a weapon is equipped (`Constants.FIRST_PERSON_AIM`, `GunController`, restores the pre-lock `MouseBehavior` so `MovementController`'s LeftControl lock is preserved). `FIRST_PERSON_AIM.REQUIRE_ACTIVE_PHASE` (default `false`) additionally gates it on the ACTIVE round phase when set true. The fixed centre crosshair is hidden during hipfire (`CrosshairUI:SetHipfireActive`); the floating gun-direction reticle is the hipfire crosshair. `CrosshairUI:SetUserEnabled(bool)` is a developer override (toggled by a button in `DummyDebugUI`) that hides both reticles entirely. ADS input (MB2) is **hold-to-aim** by default (`Constants.ADS_HOLD_TO_AIM`; set false for press-to-toggle) — `GunController` drives it through one `requestAiming(active)` helper. Client-only: no server, no remotes, no ammo/damage/hit-validation change (server still re-raycasts the client origin+direction). Not runtime-verified — see TECHNICAL_DEBT (yaw sign, cursor lock, per-gun feel need an in-game test).
- Free-aim rotation fix (2026-09-09): in `ViewModelController`'s `freeAimCF` the mouse-flick inertia (`vmMouseInertia`, magnitude-clamped to ≈26–30) is now normalised to ≈[-1,1] before it feeds the roll/translation terms, so it no longer outweighs the ≈1-scale reticle offset ~30× and drown the muzzle rotation in a lateral slide. Rotation (`freeAimYaw`/`freeAimPitch` pointing the muzzle through the reticle) is the primary hipfire effect; the normalised inertia also adds a trailing rotational swing gated by `Constants.FREE_AIM_VIEWMODEL_SWING_FACTOR`. One new constant; no new remotes/server/camera/damage changes. Not Studio-verified.
- Stage 1 first-person muzzle FX (2026-09-09): lives entirely inside `src/client/ViewModelController.lua` (`resolveMuzzleProfile` / `createMuzzleFxEmitters` / `setupMuzzleFx` / `clearMuzzleFx` / `ViewModelController.PlayMuzzleFlash`), tuned by `Constants.MUZZLE_FX` in `src/shared/Constants.lua`. Local-only, visual-only: a brief ParticleEmitter flash + tiny smoke puff + small spark burst + a ~35 ms PointLight pulse on the LOCAL player's viewmodel when they fire. Triggered from the existing fire visual path — `PlayMuzzleFlash()` is called at the end of `ViewModelController:PlayFireAnimation()`, so `GunController` was NOT changed. Emitters/light are built once per equipped viewmodel (in `EquipWeapon`, re-run lazily by `PlayMuzzleFlash` if refs are missing) and are children of the viewmodel Model, so they are destroyed with it; `clearMuzzleFx()` (called from `init` and `HolsterWeapon`) only drops references and bumps a token so any pending `task.delay` light callback becomes a no-op. Per-gun: `Constants.MUZZLE_FX.PROFILES[<weaponName>]` is merged over `.DEFAULT`, so each weapon's flash/smoke/spark/light can differ (AKS74 and AR15 profiles provided). Expected authored attachment name: **`MuzzleAttachment`** (searched for by the names in `MUZZLE_FX.MUZZLE_ATTACHMENT_NAMES`); if absent it is auto-created on a BasePart named "Barrel"/"Muzzle" (verified on the AKS-74 viewmodel, which has a `Barrel` part and no attachment) with a one-time Logger.warn. No new remotes, no server changes, no combat/ammo/reload/damage changes, no camera writes.
- Stage 1 local bullet impact FX (2026-09-09, material-specific 2026-09-10): owner is the `ImpactFX` helper table **inside `src/client/GunController.lua`** (extraction to a standalone `ImpactFXController.lua` is filed in TECHNICAL_DEBT — unblocked but not done). Tuned entirely by `Constants.BULLET_IMPACT_FX` in `src/shared/Constants.lua`: `MATERIAL_CATEGORY` (`Enum.Material` → `concrete` / `metal` / `wood` / `dirt` / `default`) + a `CATEGORIES` table with a `DUST` / `DEBRIS` / `SPARK` sub-table per category (missing sub-table = that emitter is silent for the category). On a local shot whose client raycast returns a hit, `attemptFire()` calls `ImpactFX.PlayImpact(hitResult.Position, hitResult.Normal, hitResult.Material)` — a short material-specific burst (gray dust + chips for concrete; bright directional sparks + smoke wisp for metal; tan splinter puff + chips for wood; subtle dirt puff for dirt/grass; neutral dust for anything unknown or nil) at the **predicted/local** hit point, the rig oriented so `+Y` = surface normal. Fixed pre-built pool of `BULLET_IMPACT_FX.POOL_SIZE` (24) anchored, `CanQuery=false`, invisible parts under a `workspace/BR_ImpactFx` folder, each carrying three `ParticleEmitter`s (Dust / Debris / Spark) on an `Attachment`; `ensureInit()` pre-resolves every category's `NumberRange`/`NumberSequence`/`ColorSequence` once so `PlayImpact` allocates nothing per shot. `PlayImpact` reuses the free/soonest-free rig so **part count is bounded and never grows on full-auto**. Cleanup is time-based (a rig frees after `IMPACT_PART_LIFETIME`), no stored connections. Textures are engine built-ins (smoke sprite for dust, blank square for debris/spark). The FX is **local-only and visual-only**: not replicated, never consulted for damage/ammo/hit validation, no camera writes; the server still re-raycasts the client origin+direction independently. Glass and persistent decals/bullet-holes are not implemented. No new remotes, no server changes.
- ADS zoom-only mode (2026-09-09): `Constants.VIEWMODEL_ADS_ANIMATION_ENABLED` (default **false**) makes `ViewModelController:SetAiming` skip the `adsIn`/`adsIdle`/`adsOut` pose clips and go straight Hip→Aiming→Hip. The existing `adsAimAlpha` pivot blend still glides the viewmodel to the camera-centred position; the FOV zoom is unchanged and stays owned by `MovementController.updateSprintFov` ("Task A", `camera.FieldOfView` → `Constants.CAMERA_ADS_FOV`, keyed off `isAiming` — still the only FOV writer). `PlayADSFireAnimation` delegates to `PlayFireAnimation` in this mode (normal hipfire kick for shots while aimed). Set the constant true to restore the full authored ADS animation sequence + its bounded-recovery watchdogs; the four ADS tracks are loaded by `_setupWeaponAnimations` either way. Client/viewmodel only — no new remotes, no server/damage/ammo/fire-rate change, no new camera writer.
- Pre-round loadout menu (2026-09-10): new `src/StarterPlayer/StarterPlayerScripts/Controllers/UI/LoadoutMenu.lua` (client UI, ClientInit slot 15) + new `src/ServerScriptService/Services/LoadoutService.server.lua` (both mapped in `default.project.json`) + one new RemoteEvent **`SelectLoadout`** (client → server, producer `LoadoutMenu`, consumer `LoadoutService`; declared in `RemoteSetup.server.lua`). `LoadoutService` is the sole writer of two per-player attributes — **`BR_LoadoutPrimary`** (a `WeaponData` key) and **`BR_TeamPref`** (`Auto` / `Attackers` / `Defenders`) — validated against `Constants.LOADOUT` (new tuning table: toggle key `M`, `AUTO_OPEN_PHASES` = LOBBY + RESULTS, `WEAPONS` allowlist, `LOCK_EDITS_DURING_ACTIVE = false`). Readers: `GunService.resolvePrimary()` (weapon stats / ammo / `AmmoChanged` label / damage `sourceName`, falls back to `Constants.DEFAULT_WEAPON`), `TeamService.resolveTeamAssignment()` (honours the team pick in `assignTeams()`, even-split fallback, never leaves a side empty with 2+ players), and `GunController`'s equip path (which viewmodel to equip, falls back to `Constants.DEFAULT_VIEWMODEL_WEAPON`). Only **AKS74** is selectable; `AR15` / `SCAR` are greyed "LOCKED" rows (no full `WeaponData` yet). The panel also carries a **CROSSHAIR: ON/OFF** button (2026-09-10) wired to `CrosshairUI:SetUserEnabled` — the player-facing equivalent of the dev-only `DummyDebugUI` crosshair button; session-local, re-synced from `CrosshairUI:IsUserEnabled()` on each open. Partially closes DEBT-013. `WeaponData.lua` / `WeaponFeel.lua` were **not** modified. `MatchService` phase loop unchanged (menu reacts to the existing `RoundStateChanged` feed). The panel also carries a **RESPAWN BOTS** button (2026-09-10, Stage 1H) — fires the new **`RespawnBots`** RemoteEvent (client → server, declared in `RemoteSetup.server.lua`, no payload); `AIService` listens for it and calls its own `AIService.RespawnAllSquads()` (instantly destroys every live/pending-cleanup grunt, no ragdoll — a reset, not a kill — then re-spawns fresh squads at `Workspace/AISpawns`), gated by one shared server-wide cooldown (`Constants.AI.RESPAWN_COOLDOWN_SECONDS`, not per-player) so no single player can spam-reset the AI for everyone. Available to every player, Studio and published alike. The button's own countdown is a cosmetic client-side mirror of that cooldown, not the real gate — a stale/missed client timer can never let an early request through, the server silently drops it. Not runtime-verified — see TECHNICAL_DEBT. **Cursor-sync fix + mid-round weapon switch (2026-09-10):** `setOpen`'s cursor-restore is now `syncCursorToCameraMode()`, also driven by `LocalPlayer:GetPropertyChangedSignal("CameraMode")` — not just the moment the menu closes — closing a timing race against `ViewModelController` flipping `CameraMode` on respawn. Separately, `GunController` now re-equips instantly (holster + re-equip through its own existing branches) when `BR_LoadoutPrimary` changes while the player already holds a weapon in `ACTIVE`, and `GunService` mirrors it by re-running `setupAmmo` for the new weapon on the same attribute change — Deploy previously only took effect at the next PREP. The `GunController.lua` / `GunService.server.lua` additions are **left uncommitted**: both files are mid-rewrite by a parallel shotgun-support session and could be overwritten by its next save. `LoadoutMenu.lua` (this session's own file) is committed as usual. **KILL ALL BOTS button (2026-09-11):** new **`KillAllBots`** RemoteEvent (client → server, declared in `RemoteSetup.server.lua`, no payload) + `AIService.KillAllBots()` — unlike `RespawnAllSquads`, sets every live grunt's `Humanoid.Health = 0` directly, routing each through the existing per-NPC `Died` connection → `onNPCDied` (ragdoll, blood, `DEATH_CLEANUP_DELAY` corpse timer, attack-slot release, squad formation reassignment — the same real death path a player's bullet uses) and does **not** spawn replacements. Gated by its own shared server-wide `Constants.AI.KILL_ALL_COOLDOWN_SECONDS` cooldown, same not-per-player pattern as Respawn Bots; the button's countdown is likewise a cosmetic client-side mirror. `AIService.server.lua` / `RemoteSetup.server.lua` / `LoadoutMenu.lua` / `Constants.lua` only — no other file touched, no client files beyond the existing `LoadoutMenu`.
- AI Stage 1A — server-owned squad NPC foundation (2026-09-10): new `src/ServerScriptService/Services/AIService.server.lua` (self-running Script, mapped in `default.project.json` next to the other Services; there is no `ServerInit`). New `Constants.AI` tuning table. **No new remotes, no client AI scripts, no client damage decisions.** Ownership: `AIService` owns every AI decision (spawning, patrol, detection, chase, shooting, death cleanup) server-side. Studio-placed inputs it *reads* (never creates or edits): `Workspace/AISpawns` (BasePart squad origins) and `Workspace/AIPatrolPoints` (BasePart patrol destinations) — missing/empty folders trigger one `Logger.warn` and the service still starts. Runtime output: `Workspace/AI`, a Folder holding the live NPC models; `AIService.Destroy()` removes it. Each NPC is a generated R6 rig (geometry mirrors `scripts/Build-TestDummy.luau`), named `Constants.AI.NPC_NAME_PREFIX .. "_<squad>_<n>"`, carrying attribute `BR_AINpc` and tag **`Constants.TAG_DAMAGE_ENTITY`**. States: **Idle / Patrol / Chase / Attack / Dead** — Idle/Patrol move between patrol points (or hold near the squad spawn), Chase pursues the nearest visible player (server raycast LOS), Attack holds at range and burst-fires server raycasts, Dead stops thinking and the model is `Destroy()`ed after `Constants.AI.DEATH_CLEANUP_DELAY`. Damage integration uses **existing** paths only, no `GunService`/`DamageService` edit: player→AI works because the `TAG_DAMAGE_ENTITY` tag routes bullet hits through `GunService.getDamageableEntity` → `DamageService:ApplyDamage`; AI→player calls the public `DamageService:ApplyDamage({ attacker = nil, region = Unknown, ... })` for a flat `Constants.AI.SHOT_DAMAGE`. `BloodService` reacts to any `CombatEvents.DamageDealt`, so hits on/by AI already bleed (side effect). If `autoSpawnEnabled()` and `AISpawns` has parts, up to `MAX_SQUADS` squads spawn on server start; active NPC count is hard-capped at `MAX_ACTIVE_NPCS`. `autoSpawnEnabled()` is `Constants.AI.SPAWN_ON_SERVER_START_IN_STUDIO` in Studio, `Constants.AI.SPAWN_ON_SERVER_START_IN_PUBLISHED` on a published server (**fixed 2026-09-10** — the flag did not exist and the gate was hard Studio-only, so a published server built the `AISpawns`/`AIPatrolPoints` parts via `TestAreaBuilder` but `AIService` never spawned anyone into them; both default `true`). `AIService` connects `workspace.ChildAdded` (its one persistent connection, cleaned in `Destroy()`) and re-scans if `AISpawns`/`AIPatrolPoints` appear after it starts, so server-script order does not matter and `TestAreaBuilder` (which builds those folders — see the test-area entry) can run in either order. `PathfindingService` is not used (plain `Humanoid:MoveTo`). No ragdoll, rewards, killstreaks, AI types, animations, or custom weapon models. **AI Stage 1B — combat feedback FX (2026-09-10):** new `Constants.AI_COMBAT_FX` + `AIService` helpers `setupAICombatFx` / `playAIShotFx` / `playAITracer`. When a grunt fires, `fireOneShot` (after the raycast, hit or miss — the damage path is unchanged) plays **server-created, world-replicated placeholder FX owned by AIService**: a muzzle flash + smoke puff + light pulse, an optional short tracer `Beam`, and a 3D gunshot `Sound`. The muzzle attachment (`AIMuzzleAttachment`) is **auto-created on the grunt's Right Arm, or HumanoidRootPart if absent** — a placeholder until AI weapon models exist. Emitters / light / sound are built **once per NPC** (in `setupAICombatFx`, called from `spawnOne`) and reused; only the tracer makes a temporary holder Part per shot, `Debris`-cleaned, parented under `Workspace/AI` (also swept by `Destroy()`). Placeholder asset IDs (`rbxassetid://0` for flash/smoke/sound) are tolerated and warn once. **AI Stage 1C — grunt weapon model + third-person animation (2026-09-10):** new `Constants.AI` Stage 1C fields + `AIService` helpers `attachAIWorldWeapon` / `setupAIAnimation` / `playAIFireAnim`. Each grunt gets the **real `ReplicatedStorage/WorldModels/AKS-74`** model cloned in and welded `Handle` → `Right Arm` with a `Motor6D` named `Constants.WORLD_WEAPON_GRIP_MOTOR_NAME` — the exact attach body `WorldWeaponService` uses for players (that service takes a `Player` and is a Script, so the ~30 lines are **duplicated**, using the same `Constants.WORLD_WEAPON_*` / `WORLD_AKS74_GRIP_C0/C1` values). An `Animator` is added in `buildRig`; `setupAIAnimation` loads the `WeaponData[Constants.AI.WEAPON_NAME].animations.thirdPerson` **equip / idle / fire** clips (reload skipped — grunts have infinite ammo) plus the **default R6 idle/walk** clips (`Constants.AI.LOCOMOTION_*_ANIM_ID`), plays the weapon idle pose (required — grip C0/C1 are identity so the `idle` clip is what positions the gun), and connects `Humanoid.Running` to swap idle↔walk by speed. `fireOneShot` plays the fire clip per shot next to the Stage 1B FX; `muzzleParentFor` now puts `AIMuzzleAttachment` on the gun's **`Barrel`**. Every step is `pcall`'d and warns once on failure — a missing model or a failed `LoadAnimation` just means the grunt fires without the gun / in the raw pose. **No new remotes, no client files, no `default.project.json` change**; `WorldWeaponService` / `WeaponData` / `DamageService` / `GunService` are unchanged. **AI Stage 1D — face target + take cover (2026-09-10):** in the stationary `Attack` state grunts set `Humanoid.AutoRotate = false` and a new `faceToward` helper lerps the `HumanoidRootPart` to point the rifle at the player; walking states (Chase / Cover / Patrol) keep `AutoRotate` on and face their travel direction (a per-think CFrame write while also walking would freeze the grunt). New `Cover` state: after each burst `startBurst` arms `record.coverUntil`; while it is in the future `thinkNPC` runs `findCoverPoint` (samples `Constants.AI.COVER_SAMPLE_ANGLES` off the away-from-target vector, `Constants.AI.COVER_SEEK_DISTANCE` out, keeps the first that a raycast finds LOS-occluded, else a plain retreat point) and holds there for `Constants.AI.COVER_DURATION` before peeking out and firing again — a per-grunt peek/shoot/hide loop. All flag-gated (`FACE_TARGET`, `TAKE_COVER`); per-grunt only (no squad cover, no tagged cover objects, no pathfinding). **AI Stage 1E — fight from cover / react to fire / flank (2026-09-10):** the `Attack` branch first walks to `findFightingPosition` (a spot within `FIGHT_SEEK_DISTANCE` that keeps LOS to the target **and** has an obstacle within `COVER_ADJACENT_RADIUS`) before planting to fire — `nil` result means no cover nearby, hold in place as before. A **service-level `CombatEvents.DamageDealt` listener** (a `require`d BindableEvent — no new remote) arms `record.coverUntil` the instant a grunt takes any hit and, if a player inflicted it, makes that player the target + updates `lastSeenPos` (so a grunt shot from behind while patrolling turns, ducks, and engages). New `Search` state: `lastSeenPos` is kept after the live target ref is dropped and, once it's older than `LAST_SEEN_CHASE_SECONDS` (up to `SEARCH_DURATION`), the grunt moves in via `flankPointFor` — an arc that's wide when far from the spot and converges on it, with squad members alternating `flankSide` for a rough pincer — instead of giving up. **AI Stage 1F — crouch in cover + weapon-vs-wall (2026-09-10):** while in the `Cover` state a grunt plays **the player's own third-person `CrouchIdle` clip** (`Constants.MOVEMENT_ANIMATION_IDS.R6.Unarmed.CrouchIdle`, read at load with a `Constants.AI.CROUCH_IDLE_ANIM_ID` fallback) instead of standing; `updateWeaponCollision` runs every think and, when a forward chest-ray hits a wall within `GUN_COLLISION_DISTANCE`, additively pulls the welded gun (grip `Motor6D` C1) and the `Right Shoulder` `Motor6D` C0 back toward the body (lerped, composes on top of the animation, restores when clear) so the rifle/arm stop poking through walls; `findCoverPoint` / `findFightingPosition` results are pulled `WALL_STANDOFF` studs clear of walls via `pullFromWalls`. `buildRig` now returns the Right Shoulder motor so its rest C0 can be captured. Not runtime-verified — see TECHNICAL_DEBT. **AI Stage 1F.1 — hide on the far side of cover (2026-09-10):** `findCoverPoint` now samples a fuller ring (`Constants.AI.COVER_SAMPLE_ANGLES` widened) and, among candidates whose LOS to the player is blocked, prefers the one whose blocking obstacle is *nearest* (within `Constants.AI.COVER_HUG_DISTANCE`) so the grunt tucks against the far side of that obstacle rather than behind a distant wall; the `Cover` branch also re-picks the spot if the player flanks around it and regains LOS while the grunt is parked there. **Player first-person weapon retraction (2026-09-10):** `ViewModelController` now applies a Tarkov-style close-quarters retract — `computeWallCollisionCF` casts one forward ray from just behind the camera each `RenderStepped`; when it hits collidable geometry within `Constants.VIEWMODEL_WALL_PROBE_DISTANCE` it returns a camera-local `wallCF` (appended last in the PivotTo chain, next to the positional-recoil term) that pulls the whole viewmodel back toward the player (`VIEWMODEL_WALL_PUSH_MAX`) and tucks the muzzle up (`VIEWMODEL_WALL_TUCK_MAX_DEG`), framerate-independently lerped (`VIEWMODEL_WALL_LERP_SPEED`) and scaled down while ADS (`VIEWMODEL_WALL_ADS_SCALE`). Cosmetic only — `GunController` still fires from the camera / free-aim solve, so shooting point-blank into a wall is unchanged. Client/viewmodel only: no new remotes, no server/damage/camera-writer change. Not runtime-verified. **AI Stage 1G — death ragdoll (2026-09-10):** `AIService` now `require`s the existing `RagdollService` and, on `Humanoid.Died`, calls `RagdollService:Apply` on the grunt with a knockback impulse built from `record.lastHit` (the latest accepted `CombatEvents.DamageDealt`, looked up in `Constants.RAGDOLL_WEAPON_IMPULSE` by `sourceName` else `RAGDOLL_IMPULSE_DEFAULT`) — the same path as `DummyService.onDummyDied`. The welded `EquippedWorldWeapon` model **and its grip `Motor6D`** (which lives on the Right Arm) are destroyed first, because the AKS-74 world model joins its 9 parts with `Motor6D`s that `RagdollService`'s descendant sweep would convert to ball sockets and scatter. Corpse still despawns after `Constants.AI.DEATH_CLEANUP_DELAY`; gated by `Constants.AI.RAGDOLL_ON_DEATH`. Blood already worked (`BloodService` reacts to the same `DamageDealt` for any target). Verified teardown + ragdoll in a live Server datamodel; in-game feel not play-tested. `AIService` + `Constants.AI` only — no new remotes, no `RagdollService` / `DummyService` / `DamageService` / `BloodService` edit. **AI Stage 1H — combat realism pass (2026-09-10):** `fireOneShot` now widens `SHOT_SPREAD_DEGREES` by a per-grunt `record.aimSkill` (rolled once at spawn, `Constants.AI.AIM_SKILL_VARIANCE`, clamped `[AIM_SKILL_MIN, AIM_SKILL_MAX]` — not every grunt shoots identically) plus a bonus scaled by the target's `AssemblyLinearVelocity` (`AIM_MOVING_TARGET_SPREAD_*` — harder to hit someone actually sprinting/strafing). A new `record.engageAtClock` gates `startBurst` in the planted `Attack` branch: acquiring a **new** live target (a fresh sighting, or a different attacker on a from-behind hit via the `DamageDealt` listener) arms a random `REACTION_TIME_MIN`–`MAX` beat before the first shot — the grunt still tracks/faces the target during that beat, it just doesn't fire instantly; re-confirming the *same* target on every recheck or re-peeking from `Cover` does not re-arm it, so an ongoing firefight never stutters. New `coverDurationFor()` multiplies `COVER_DURATION` by `LOW_HEALTH_COVER_MULTIPLIER` once `Humanoid.Health / MaxHealth` drops to `LOW_HEALTH_RATIO` — a wounded grunt stays hidden longer than a fresh one on the same timer; used by both `startBurst`'s post-burst cover-arm and the hurt-reaction listener. No pathfinding/tactics change, no reload (still infinite ammo, per the Stage 1C decision), no grenades — `Constants.AI` + `AIService` only, not runtime-verified. **AI Stage 1C — fair combat tuning (2026-09-10; second use of the "1C" label — distinct from the earlier grunt weapon-model/animation entry above, see docs/TECHNICAL_DEBT.md "AI Stage 1C" for the naming note):** new `Constants.AI_COMBAT_TUNING` table + `AIService` helpers `armReaction` / `aimBandFor` / `updateSquadAttackSlots`, plus new `NPCRecord`/`SquadRecord` fields. **Reaction delay** is now a 3-tier system keyed off `armReaction(record, now)`, called only when a grunt's live target genuinely changes (fresh sighting, or a new attacker via the hit-reaction listener): `RecentlyDamaged` (fastest — `record.recentlyDamagedUntil` in the future) beats `Alert` (`squad.alerted == true`, sticky and squad-wide — one grunt spotting a threat puts the whole squad on alert) beats `Unaware` (first contact); `record.reactionReadyAt` gates `startBurst` in the planted `Attack` branch (movement/facing continue during the delay). **This supersedes** the Stage 1H flat `Constants.AI.REACTION_TIME_MIN/MAX` fire-gate (`record.engageAtClock` / the old `reactionDelay()` helper were removed) — those two constants are left in Constants.lua, now unread, per the task's "do not remove existing Constants.AI values." **Aim ramp**: `fireOneShot` multiplies its spread by a ramp factor from `Constants.AI_COMBAT_TUNING.INITIAL_SPREAD_MULTIPLIER` (wide, at first sight) toward `FINAL_SPREAD_MULTIPLIER` (tighter than base) as `record.visibleTargetTime` (continuous LOS on the current target, frozen the instant LOS breaks, reset only once the break exceeds `LAST_KNOWN_POSITION_MEMORY`) approaches `AIM_SETTLE_TIME` — layered on top of, not replacing, the Stage 1H per-grunt `aimSkill` and moving-target spread bonus (three independent multiplicative factors on the same cone). **Suppression**: the `CombatEvents.DamageDealt` listener now also arms `record.suppressedUntil` (`SUPPRESSED_SPREAD_MULTIPLIER` applied in `fireOneShot` while active) and `record.recentlyDamagedUntil` (feeds the reaction tier above) on every accepted hit; near-miss suppression (no direct hit) was left out — no existing signal exposes a "shot passed close by" event without a new system, and the spec marked it optional. **Target memory**: a new `record.lastKnownTargetPosition` mirrors the existing `lastSeenPos`; the pre-existing Chase/Search/flank timing (`LAST_SEEN_CHASE_SECONDS`, `SEARCH_DURATION`, `flankPointFor`) is unchanged — `LAST_KNOWN_POSITION_MEMORY` governs the aim-ramp decay above instead of a second parallel chase timer, to avoid two competing "how long do I remember you" systems. Grunts still only fire with `canSee(...) == true` (unchanged structural guarantee — `fireOneShot` is only reachable from the LOS-gated planted-`Attack` branch), so "no shooting through walls off last-known position" was already true and needed no new code. **Per-squad attacker limit**: `updateSquadAttackSlots` (rechecked at most every `ATTACK_SLOT_RECHECK_INTERVAL`, sticky — an existing holder keeps its slot as long as it stays eligible, only vacated slots are refilled) caps live attackers at `MAX_SIMULTANEOUS_ATTACKERS_PER_SQUAD`; grunts without a slot stay planted and aimed but hold fire. **Damage-rate safety**: `record.lastDamageCallAt` blocks a `DamageService:ApplyDamage` call inside `MIN_TIME_BETWEEN_DAMAGE_CALLS` of the previous one, independent of burst timing. `Constants.AI_COMBAT_TUNING.ENABLED = false` cleanly bypasses the whole pass (immediate reaction, no ramp/suppression multiplier, no slot limit) without touching any other AI system. Kept the existing `NPCRecord.target` field name rather than the spec's `currentTarget` (same role; renaming ~15 call sites was out of scope for a tuning-only pass). `AIService` + `Constants.AI_COMBAT_TUNING` only — no new remotes, no client files, `GunService` / `DamageService` / `RemoteSetup` / `default.project.json` untouched. MCP-checked the tier selection, aim-ramp formula, sticky slot allocation, target-switch cooldown, and visible-time freeze/reset math; not runtime-verified in Play. **AI squad spacing / anti-bunching (2026-09-11):** new `Constants.AI_SQUAD_SPACING` table + `AIService` helpers `assignFormationSlots` / `ensureFormationAssigned` / `formationDirectionForSlot` / `squadSpreadGoal` / `getSeparationAdjustedGoal` / `issueSquadMoveGoal`, plus new `NPCRecord` fields (`formationSlotIndex`, `currentMoveGoal`, `lastMoveGoalAt`, `lastMoveAnchor`) and `SquadRecord` fields (`members`, `leader`, `formationSlotAssignments`, `lastFormationAssignAt`). **Formation slots:** `assignFormationSlots` (called once at spawn, at most every `FORMATION_SLOT_REASSIGN_INTERVAL` thereafter, and immediately on a member's death — never every think) gives each living member a slot index: 1 = leader/anchor (the original spawn leader if alive, else the first survivor is promoted), 2+ cycle through `SLOT_OFFSETS`' flanking/rear directions. **Squad anchor:** the leader's position if alive, else the caller uses the average of the living members implicitly via each member's own goal (no single explicit "anchor position" field is computed — see TECHNICAL_DEBT for the exact scoping). **Anti-bunching movement:** `issueSquadMoveGoal` — called from Patrol/Idle, Chase, the fresh (non-stale) Search branch, the Attack transit-to-fighting-spot branch, and the Cover retreat-to-hide-spot branch, **not** the stationary `MoveTo(root.Position)` "hold position" calls elsewhere, which stay direct/unthrottled — only recomputes/reissues `Humanoid:MoveTo` when `MOVE_GOAL_RECALCULATE_INTERVAL` has elapsed or the underlying raw target has moved past `MOVE_GOAL_JITTER` studs (comparing the RAW anchor, not the final jittered goal, so the throttle isn't defeated by jitter re-rolling every think). For shared-target cases (Chase/Search-fresh/Patrol/Idle) it fans squadmates out from the anchor by formation-slot direction × a context spread radius (`CHASE_SPREAD_RADIUS` / `IDLE_SPREAD_RADIUS`) plus jitter via `squadSpreadGoal`; for already-individually-computed goals (Attack fighting spot, Cover hide spot, the Stage 1E flank point) it applies only `getSeparationAdjustedGoal` (nudges a goal away from any living squadmate closer than `MIN_PERSONAL_SPACE`, pure math, never touches `HumanoidRootPart` or applies a physics force) with no added spread/jitter, so it never pulls a grunt off a validated cover/LOS spot. `COMBAT_SPREAD_RADIUS` is used specifically as the `Attack` fallback when `findFightingPosition` finds no literal cover — instead of planting wherever `ATTACK_RANGE` happened to be reached, the grunt fans out around the target by formation slot; the crouch decision at the plant point now re-checks `hasNearbyCover` live rather than just "a fight point exists," since that fallback point has no cover guarantee. Falls back to the pre-existing Stage 1A static ring offset (`record.slot`) when `Constants.AI_SQUAD_SPACING.ENABLED` is false or a grunt's squad record can't be found, cleanly reverting to the prior behavior. `AIService` + `Constants.AI_SQUAD_SPACING` only — no new remotes, no client files, `GunService` / `DamageService` untouched, spawning/patrol/chase/attack/cover/combat-FX/damage/death-cleanup preserved. MCP-checked the formation-slot assignment (leader promotion, slot numbering), separation push math, and the throttle's anchor-vs-jitter distinction in isolation; not runtime-verified in Play. **Crouch-on-arrival + crouch-fire fix (2026-09-10):** the `Cover` and `Attack` planted branches now call `setCrouched` only once `(root.Position - spot).Magnitude <= AI.FIGHT_ARRIVE_DIST` — a grunt stands (runs) on its way to a cover / fighting spot and only drops into the crouch pose on arrival, fixing the reported "gets shot, instantly crouches, waddles to cover" look. `Attack` also no longer forces standing to fire: once planted at real cover (`findFightingPosition` found a spot) it crouches **and** fires from the crouch (nothing in `startBurst`/`fireOneShot` was ever gated on pose); with no cover nearby it still stands in the open. Reuses the existing `FIGHT_ARRIVE_DIST` constant for both branches' arrival check rather than adding a new one. `AIService` + a `Constants.AI.CROUCH_IN_COVER` comment update only — no new remotes/files, not runtime-verified. **Covering fire while retreating (2026-09-10):** the `Cover` branch's not-yet-arrived path is now a bounding-overwatch loop — walk toward the hide spot, periodically stop (`AutoRotate = false`, `faceToward`) and fire a burst via `startBurst` (its internal loop relaxed from `state ~= "Attack"` to also allow `"Cover"`), then resume walking once the burst ends; `Constants.AI.COVER_RETREAT_FIRE` / `COVER_RETREAT_SHOT_MIN/MAX` gate it. Deliberately never combines a `faceToward` write with an active `MoveTo` in the same think (that combination froze grunts in the original Stage 1D bug) — reuses the same "stop fully, then face+fire" pattern `Attack` already uses. Once actually arrived at the hide spot, behavior is unchanged (goes fully quiet). Not squad-attack-slot-limited (see docs/TECHNICAL_DEBT.md "AI Stage 1F"). `AIService` + `Constants.AI` only, not runtime-verified. **AI squad fire discipline (2026-09-11):** new `Constants.AI_FIRE_DISCIPLINE` table + `AIService` helpers `attackSlotEligible` / `canNpcUseAttackSlot` / `releaseAttackSlot`, plus new `NPCRecord` fields (`hasAttackSlot`, `attackSlotAssignedAt`, `lastSupportRepositionAt`). **Consolidates in place, rather than duplicates,** the per-squad attack-slot mechanism from the earlier "fair combat tuning" pass: `SquadRecord.attackerSlots`/`nextAttackSlotRecheck` were renamed to the fire-discipline task's required `activeShooterIds`/`lastAttackSlotUpdateAt`, and `updateSquadAttackSlots(squadRecord, now)` (same name, now asserting both args) was rewritten to add real eligibility — `attackSlotEligible` now requires a live target, in `ATTACK_RANGE`, **and line of sight** ("no wallhack shooting" — the old version only checked `state == "Attack"` and a non-nil target) — plus `ATTACK_SLOT_TIMEOUT`, a hard release after holding a slot too long even if still eligible, so one grunt can't hog it forever. `canNpcUseAttackSlot(npcRecord)` is the exact-signature query helper: eligible AND (already holds a slot OR the squad has room). Slots are released immediately (not on the next recheck) via a new `releaseAttackSlot` hook inside `setState` on every transition OUT of `"Attack"`, and explicitly in `onNPCDied` (which sets state directly, bypassing `setState`). **Non-shooters reposition instead of standing still:** a grunt without a slot still `faceToward`s the target (`WAITING_BOT_CAN_TRACK_TARGET`) and, at most every `NON_SHOOTER_REPOSITION_INTERVAL_MIN`–`MAX` seconds (`WAITING_BOT_CAN_CHASE`), picks a new support point — either holding farther back at `HOLD_ANGLE_DISTANCE_MIN`–`MAX` studs (watching, not closing in) or moving up to `SUPPORT_MOVE_DISTANCE_MIN`–`MAX` studs (supporting), chosen randomly each cycle, direction from the squad-spacing formation slot — routed through `issueSquadMoveGoal` (radius 0: the support spot is already final) so it inherits the existing separation-avoidance and MoveTo throttling for free. `WAITING_BOT_CAN_SHOOT` (false by default) is an explicit escape hatch that lets non-shooters fire anyway, bypassing the slot limit entirely, for future difficulty tuning. `Constants.AI_COMBAT_TUNING.MAX_SIMULTANEOUS_ATTACKERS_PER_SQUAD`/`ATTACK_SLOT_RECHECK_INTERVAL` are left in `Constants.lua`, now unread (same values duplicated under the new table, which `AIService` reads as authoritative) — see docs/TECHNICAL_DEBT.md "AI squad fire discipline" for that and every other scoping note. `AIService` + `Constants.AI_FIRE_DISCIPLINE` only — no new remotes, no client files, `GunService`/`DamageService` untouched, `DamageService` integration and burst timing for active shooters unchanged. MCP-checked slot assignment/drop/timeout and the support-distance random selection in isolation (including fixing an off-by-zero throttle bug found during that check — see TECHNICAL_DEBT); not runtime-verified in Play. **AI squad awareness / last-known-position memory (2026-09-11):** new `Constants.AI_SQUAD_AWARENESS` table + `AIService` helpers `shareSquadAlert` / `clearStaleSquadAlert`, plus new `NPCRecord` fields (`isAlertedBySquad`, `investigateGoal` — the required `lastSawTargetAt`/`lastKnownTargetPosition` fields already existed from the earlier "fair combat tuning" pass and are reused, not duplicated) and new `SquadRecord` fields (`alertUntil`, `lastKnownTargetPosition`, `lastKnownTargetPlayer`, `lastKnownUpdateAt`, `lastContactAt`). **Alert sharing:** `shareSquadAlert`, called every re-confirmed sighting (not just first acquisition) from the target-acquisition block and from the `CombatEvents.DamageDealt` hit-reaction listener, refreshes the squad's shared `alertUntil`/`lastContactAt` and — throttled to `LAST_KNOWN_POSITION_SHARE_INTERVAL`, and only when a position is actually known (a hit from an unknown attacker still raises the squad's alert without a location, per spec) — updates `lastKnownTargetPosition`/`lastKnownTargetPlayer` and marks living squadmates within `ALERT_SHARE_RADIUS` as `isAlertedBySquad`. **This supersedes** the earlier Stage 1C sticky `squad.alerted` boolean for reaction-tier selection: `armReaction` now checks `now < squad.alertUntil` instead, so a squad's fast "Alert" reaction tier genuinely expires rather than lasting the squad's whole life — `alerted` is left declared in `Constants`/the type, now unread. **Investigate goals:** a grunt with no target and no own recent sighting, but `isAlertedBySquad` and a squad `lastKnownTargetPosition` still fresh (within `LAST_KNOWN_POSITION_MEMORY`) and past its own `LOSE_TARGET_GRACE_TIME`, picks a random offset (`INVESTIGATE_DISTANCE_MIN`–`MAX`, re-picked on `INVESTIGATE_ARRIVE_DISTANCE` arrival or the squad-spacing recalculate interval — never the exact shared point) and moves there via `issueSquadMoveGoal` (radius 0: separation + throttle only, inheriting anti-bunching for free) — reuses the `"Search"` `AIState` rather than adding a dedicated one. **Alert expiration:** `clearStaleSquadAlert`, called every think per squad (cheap early-return), drops the shared position and every living member's `isAlertedBySquad`/`investigateGoal` once `CLEAR_ALERT_AFTER_NO_CONTACT` seconds pass with no sighting/hit from any member — the squad genuinely returns to Patrol/Idle, not aggro forever. **No wallhack shooting, by construction, not by toggle:** `attackSlotEligible` (fire discipline) already hard-requires the shooter's own `canSee(...)`, unconditionally, regardless of `REQUIRE_OWN_LOS_TO_SHOOT`'s value — shared awareness only ever changes where a grunt walks, never whether it can pull the trigger; that flag exists to document the invariant, not toggle it. `AIService` + `Constants.AI_SQUAD_AWARENESS` only — no new remotes, no client files, `GunService`/`DamageService` untouched, spawning/spacing/fire-discipline/chase/attack/combat-FX/damage/death-cleanup all preserved. MCP-checked the share/throttle/broadcast logic, the alert-clear pre-filter, the reaction-tier switch to `alertUntil`, and the investigate-goal recompute conditions in isolation; not runtime-verified in Play.
- Developer test area (2026-09-09): `src/ServerScriptService/Services/TestAreaBuilder.server.lua` (mapped in `default.project.json`; new `Constants.DEV_TEST_AREA` layout table) owns a generated developer sandbox at `Workspace/BrokenReality_TestArea`. It runs on server start and **no-ops unless `DEV_TEST_AREA.ENABLED == true` AND it is allowed to run here — always in Studio, and in a published server only when `DEV_TEST_AREA.RUN_IN_PUBLISHED == true`** (added 2026-09-10; default `true` right now so a published test build spawns you in the area, ⚠ flip to `false` before a real release or the sandbox + redirected match spawns ship to live players). Rerun-safe: it destroys and rebuilds **only its own folder**, never reading or modifying any other Workspace content, and it never writes `game.Lighting`. Sections (each a child Folder): `ShootingRange` (firing line + static targets at `DEV_TEST_AREA.TARGET_DISTANCES` with distance labels + neon bullseyes + backstop), `ImpactWall` (side wall for impact-FX / spread testing), `MovementCourse` (`SprintLane` with 10-stud markers, `CrouchTunnel` with `CROUCH_TUNNEL_OPENING_HEIGHT` clearance, `Vaults` = Low/Medium/TooTall bars at the configured heights, `DropTest` platforms at `DROP_TEST_HEIGHTS` with ramps, a short `Stairs` run), `MaterialTest` (Concrete/Metal/Wood/Grass wall+floor samples — **visual surfaces only, no material-specific impact logic**), `LightingTest` (self-contained PointLight / SpotLight / shade-overhang props). Plus a baseplate and `TestSpawn` (an enabled neutral `SpawnLocation`). `BillboardGui` labels when `DEV_TEST_AREA.DEBUG_LABELS`. **Not a gameplay system**: no RemoteEvents, no per-frame loops, no connections, and no combat / damage / ammo / movement / camera / viewmodel / animation-ID changes. Since the live game uses `Players.CharacterAutoLoads = false` + `TeamService` teleports to `Workspace/Spawns` BaseParts, `TestSpawn` alone does not redirect round spawns — that is what `DEV_TEST_AREA.REDIRECT_TEAM_SPAWNS` (default true, Studio only) does: `TestAreaBuilder.redirectTeamSpawns()` stashes each `Workspace/Spawns` BasePart's original `CFrame` in the `BR_TestAreaOriginalCFrame` attribute and grids them around `SPAWN_POSITION`, restoring from that attribute first on every run so flipping the flag off + Play once fully reverts. It writes only `BasePart.CFrame` + that attribute — never reparents/resizes/destroys — and does not touch `TeamService`/`MatchService`. **AI patrol zone (2026-09-10):** `DEV_TEST_AREA.SPAWN_AI_ZONE` (default true) → a marked "AI Patrol Zone" pad inside the test-area folder + the top-level `Workspace/AISpawns` (2 parts) and `Workspace/AIPatrolPoints` (4 parts, walked as a loop) that `AIService` reads, so grunt squads spawn and patrol on Play with no manual setup. Both folders carry `BR_TestAreaOwned`; a rebuild destroys only folders with that attribute, so a hand-authored `AISpawns`/`AIPatrolPoints` is left alone (builder skips its own). `SPAWN_AI_ZONE = false` + one more run removes the owned folders. **Damage test dummies (2026-09-10):** `DEV_TEST_AREA.SPAWN_DUMMIES` (default true, Studio only) → `TestAreaBuilder.spawnTestDummies()` builds `DUMMY_COUNT` standard R6 rigs downrange of the firing line (geometry mirrors `scripts/Build-TestDummy.luau`), tagged `Constants.TAG_DAMAGE_DUMMY` + a `BR_TestAreaDummy` attribute, parented to `workspace`. `DummyService` (unchanged) then owns them — damage pipeline, blood, hit reactions, ragdoll, respawn. On each rebuild the builder first destroys every `BR_DamageDummy`-tagged model carrying the `BR_TestAreaDummy` attribute (its own rigs + `DummyService` respawn clones, which inherit it). No new gameplay code — the dummies are inert rigs.

- The initial local workspace contained no existing code or Git repository.
- Local branch: main. Origin: https://github.com/Intacet/BrokenRealityFPS.git. See GITHUB_MIGRATION.md for preservation of the previous branch/history and the upload record.
- Verbatim source capture contains 216 scripts. src/ now contains unchanged copies of 32 main scripts; pilot.project.json targets Logger only and default.project.json targets those 32 scripts. Both passed local mapping validation; neither has connected to Studio.
- Saved place and script hierarchy are inventoried below. Rojo CLI/plugin are 7.6.1; Lune is 0.10.5. Published place ID remains unconfirmed.
- Backup received: ../StudioBackups/BrokenReality_preRojo.rbxl (1,379,020 bytes). A separate migration_test copy has an identical SHA-256: 99ae27dd2262a6d5441873d60dfc356081f2521239683e2027ef3dae8023b4c8.
- All 216 source files match source decoded from the saved backup. The owner reported the test copy looks good. Exact test steps, off-device backup, and post-sync Studio checks remain pending.
- See SCRIPT_INVENTORY.md and SCRIPT_INVENTORY.json for extracted paths, classes, properties, children, attributes, tags, source hashes, and archive filenames.
- Saved script counts: ReplicatedStorage 8; ServerScriptService 11; StarterPlayer 13; Workspace 184. No saved remote instances were found; runtime setup must be inspected.
- Existing main folders: ReplicatedStorage/Modules, ServerScriptService/Services, StarterPlayer/StarterPlayerScripts/Controllers. These are candidates for review, not approved mappings.
- Existing Logger and Constants modules are present in ReplicatedStorage/Modules.
- RemoteSetup source explicitly creates 15 RemoteEvents and one RemoteFunction at runtime; zero saved remote instances does not mean the prototype has no networking.
- R6 and the direction in CURRENT_GAME_DIRECTION.md are owner-confirmed, not locally inspected.
- Shared GPT/Claude workflow: `docs/AI_WORKFLOW.md` describes the daily loop, `docs/AI_HANDOFF.md` records current state, and `docs/AI_DECISIONS.md` records durable owner decisions. VS Code tasks guard Git pulls and review diffs; they do not commit, push, connect Rojo, or publish automatically.

## Isolated city prototype — 2026-09-08

New source is under prototypes/CityDistrict; see its README for build and playtest steps. Mercer District is a fictional Richmond-inspired six-block map with 26 buildings, ground-floor routes, three objective areas, 2,494 parts, and 132 breakable windows/barriers/crates. Upper floors remain facade shells.

Generated artifacts are outside the code repo in ../CityDistrict. BrokenReality_CityTest.rbxl archives the original Workspace content inside that test copy only. The immutable preRojo backup and migrated src files remain unchanged. The test copy has its own GunService destruction integration and daylight settings. Neither Rojo configuration maps this feature; do not connect the baseline full project to the city test.

22 offline checks passed with mocked engine events/physics ownership. The assistant observed the test place opening and entering ACTIVE, but user-confirmed destruction, multiplayer, route balance, and performance verification remain pending.

## Ownership

| Content | Source of truth now | Planned handoff |
| --- | --- | --- |
| Maps, terrain, models, lighting, spawns | Studio | None for now |
| R6 rigs, animations, viewmodels, weapons/Tools and their assets | Studio | None for now |
| UI objects, sounds, effects, attachments, constraints, packages | Studio | None for now |
| Existing remotes and non-code configuration instances | Studio | None without separate review |
| Existing scripts | Studio | VS Code/Rojo individually after verified migration |
| Rules and migration records | This project | Git/GitHub backup |

## Script inventory — fill before mapping

For each Script, LocalScript, and ModuleScript record:

| Exact Explorer path | Class | Enabled/Disabled and RunContext | Attributes/tags/children | Dependencies and callers | Local file | Copy compared | Sync approved |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Pending Studio inventory | Unknown | Unknown | Unknown | Unknown | Unassigned | No | No |

Record duplicate sibling names and names that cannot map cleanly to Windows filenames. Record script.Parent chains, require paths, WaitForChild paths, object references, asset IDs, and existing remote paths. Record package links and whether scripts exist only at runtime.

Inspect ServerScriptService, ReplicatedStorage, ServerStorage, StarterPlayerScripts, StarterCharacterScripts, StarterGui, StarterPack, ReplicatedFirst, and scripts nested anywhere in Workspace/models/Tools/viewmodels. Locations are inspection targets, not assertions that scripts exist there.

## Proposed local folders — not Studio mappings

- import/studio-snapshot/: verbatim source capture organized by exact service and hierarchy; never sync this folder.
- src/: only reviewed scripts promoted for migration. Final subfolders follow the verified hierarchy; no guessed server/client/shared relocation.
- docs/: rules, inventory, decisions, and verification records.

See ROJO_REVIEW.md for the locally validated individual-script mappings. No live handoff has occurred; all scripts remain Studio-authoritative until trial verification. Scripts with children or unusual execution settings require a reviewed representation before migration.
